[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: marks tests that hit live APIs (OANDA, OpenAI, etc). Run with -m integration
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
